import 'package:flutter/material.dart';

class CustomerSpacer extends StatelessWidget {
  const CustomerSpacer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 25,
    );
  }
}
